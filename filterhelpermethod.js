let score=[12,23,4,5,54,65];
let high=score.filter((n)=>         //it is the helper method which is used check condition in filter method
                                   //from taking the values of array
{
  return n>50;
})
console.log(high);
